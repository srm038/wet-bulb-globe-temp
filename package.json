{
  "name": "wbt",
  "type": "module",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "@types/bun": "latest",
    "wrangler": "^3.78.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "test": "bun test functions/tests",
    "pages:dev": "wrangler pages dev src/public",
    "pages:deploy": "wrangler pages deploy src/public"
  }
}